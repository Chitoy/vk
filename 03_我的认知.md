vkMapMemory 在 Vulkan 中最核心的作用就是将 GPU 的内存（VRAM 或 Host-visible 的内存）映射到 CPU 的虚拟地址空间，从而允许 CPU 直接读写这块内存。
memcpy、必要的 flush、提交	真正写入字节，并让之后的设备操作可访问

举个例子（为什么必须有 Barrier）：假设你有一个 Buffer，usage 包含了 STORAGE_BUFFER_BIT 和 VERTEX_BUFFER_BIT。阶段 A： 你运行了一个 Compute Shader，往这个 Buffer 里写入了新的顶点数据（此时它是 Storage Buffer 角色）。阶段 B： 你立刻调用 vkCmdDrawIndexed，让顶点着色器读取这个 Buffer（此时它是 Vertex Buffer 角色）。如果没有 Barrier 会发生什么？虽然 usage 允许这两种行为，但 GPU 是高度并行的流水线。当阶段 B 开始读取时，阶段 A 的 Compute Shader 可能还没完全写完，或者数据还缓存在 L2 Cache 里没有刷新到显存中。这就会导致严重的数据竞争（Data Race），画面会出现闪烁或崩坏。

曙光丛林之前界面马赛克闪烁可能就是这个原因，所以需要Barrier

这段话解释了 Vulkan 中最核心、也最难理解的概念：内存依赖（Memory Dependency）。简单来说，GPU 是一个高度并行的“异步怪兽”。当你让 GPU “先写入数据 A，再读取数据 A” 时，由于缓存（Cache）的存在以及硬件并行的不可控性，后一个读取操作默认是看不到前一个写入操作的最新数据的。要让前一种写入对后一种访问“可见”，你必须显式地建立内存依赖。这句话里提到了两个核心途径，我们来逐一拆解：1. 核心概念：“可见性（Visibility）”到底在解决什么？在 GPU 内部，数据并不是直接写回显存（VRAM）的，而是先写到内部的高速缓存（比如 L1/L2 Cache）。第一步：使可用（Make Available）前一个阶段（例如 Compute Shader）把数据写到了它自己的 L1 Cache 里。此时，显存和其他着色器（例如 Vertex Shader）是拿不到这个新数据的。我们必须发布指令，把 L1 Cache 里的数据刷新（Flush）到公共的 L2 Cache 或显存中。这个过程叫“使数据可用”。第二步：使可见（Make Visible）后一个阶段（例如 Vertex Shader）准备去读数据。但它的 L1 Cache 里可能还残留着旧数据的缓存。我们必须发布指令，把它的 L1 Cache 作废（Invalidate），强迫它重新从公共的 L2 Cache 或显存中拉取最新数据。这个过程叫“使数据可见”。要完成这两步，就需要用到你提到的那两种手段：2. 手段一：正确 stage/access 的 barrier（屏障）这是最常见的单个 Queue（队列）内部或同一个 Command Buffer 内部的同步方式。当你调用 vkCmdPipelineBarrier（或 Vulkan 1.3 的 vkCmdPipelineBarrier2）时，你需要明确指定两个东西：Stage（阶段）： 什么时候做？（控制执行顺序 / Execution Dependency）Access（访问掩码）： 怎么处理缓存？（控制内存可见性 / Memory Dependency）举个实际的例子：你用 Compute Shader 写了一个 Buffer，然后马上要在图形管线的 Vertex Shader 中把它当作顶点缓冲区读取。cppVkBufferMemoryBarrier barrier{};
// 1. 前一种写入（源）
barrier.srcStageMask = VK_PIPELINE_STAGE_COMPUTE_SHADER_BIT; // Compute 阶段
barrier.srcAccessMask = VK_ACCESS_SHADER_WRITE_BIT;          // 进行了 SHADER_WRITE 写入

// 2. 后一种访问（目的）
barrier.dstStageMask = VK_PIPELINE_STAGE_VERTEX_INPUT_BIT;   // 顶点输入阶段
barrier.dstAccessMask = VK_ACCESS_VERTEX_ATTRIBUTE_READ_BIT; // 将进行顶点读取

// 3. 提交 Barrier
vkCmdPipelineBarrier(cmdBuffer, ..., 1, &barrier);
请谨慎使用此类代码。驱动看到这个 Barrier 会做什么？它会在 COMPUTE_SHADER 执行完写入后，自动把写入缓存刷新（Make Available）；并在进入 VERTEX_INPUT 阶段前，把顶点读取的缓存作废（Make Visible）。这样，后一种访问就能百分之百看到前一种写入的结果了。3. 手段二：相应提交/信号依赖（Submission / Signaling Dependency）有些时候，前一种写入和后一种访问不在同一个 Command Buffer 里，甚至不在同一个时间提交，或者在不同的 Queue（队列）上运行（例如一个在 Transfer 队列复制数据，一个在 Graphics 队列渲染）。这时候，单靠 vkCmdPipelineBarrier 就不够了，必须借助 Semaphore（信号量） 或 Fence（栅栏），也就是你提到的“提交/信号依赖”。场景：在不同的 vkQueueSubmit 中第一次提交（Submit 1）： 执行把数据写入 Buffer 的命令。你在这次提交的末尾，设置让一个 Semaphore 变成 Signaled（有信号） 状态。第二次提交（Submit 2）： 执行要读取这个 Buffer 的渲染命令。你在这次提交的配置中，设置它必须 Wait（等待） 上面那个 Semaphore。至关重要的一点： 在等待时，你必须指定 pWaitDstStageMask（等待的目标阶段）。比如指定为 VK_PIPELINE_STAGE_VERTEX_INPUT_BIT。这背后的原理是什么？Vulkan 规范规定：Semaphore 的“信号（Signal）与等待（Wait）”操作，天然自带内存可见性保证。当 Submit 1 完成，Semaphore 发出信号时，GPU 会自动保证 Submit 1 之前所有的写入操作在底层的缓存中都被刷新（Make Available）。当 Submit 2 收到信号，并到达你指定的 pWaitDstStageMask（如顶点阶段）时，GPU 会自动保证该阶段后续的所有读取操作都会看到最新的数据（Make Visible）。

Compute Pipeline 的 Pipeline Layout含义： 管线布局。它定义了当前着色器允许使用哪些插线板（哪些 Set）。GPU 首先确认：这个管线确实支持 set 0。

Compute Pipeline 的 Pipeline Layout 可以使用哪些内存块

Storage Buffer Descriptor

VkDescriptorPool


给我的感觉就是VBO 是16位为最小单元 SSBO是4位为最小单元，它俩除了这些还有什么其它的区别吗