这个是整个大的模型

                     CPU
────────────────────────────────────────────

模型数据
顶点
索引
纹理
材质
Shader
        │
        ▼
   GPU Resource
 Buffer / Image
        │
        │
        ├──── VkDescriptorSet
        │
        │
        ▼
 VkGraphicsPipeline
        │
        │
        ▼
 CommandBuffer
        │
        │ vkQueueSubmit
        ▼

────────────────────────────────────────────
                     GPU

             Command Processor
                    │
                    ▼
              Graphics Engine
                    │
        ┌───────────┴────────────┐
        ▼                        ▼
   Vertex Fetch              Pipeline State
        │
        ▼
   Vertex Shader
        │
        ▼
 Primitive Assembly
        │
        ▼
 Tessellation / Geometry
        │
        ▼
 Clip / Cull / Viewport
        │
        ▼
   Rasterization
        │
        ▼
 Fragment Shader
        │
        ▼
 Depth / Stencil
        │
        ▼
     Blending
        │
        ▼
 Render Target / Image



 
这个是gpu测的shader的模型
 先说shader 说shader各个阶段需要用的数据 比如顶点 纹理 viewport framebuffer  
   Vertex Fetch              Pipeline State
        │
        ▼
   Vertex Shader 
        │
        ▼
 Primitive Assembly
        │
        ▼
 Tessellation / Geometry
        │
        ▼
 Clip / Cull / Viewport
        │
        ▼
   Rasterization
        │
        ▼
 Fragment Shader
        │
        ▼
 Depth / Stencil
        │
        ▼
     Blending
        │
        ▼
 Render Target / Image


这个是CPU测的进程模型
 顶点数据纹理，这些数据是怎么来的？ 就是我们在CPU侧写的main.cpp 进程里面干的活，大体上可以是这样
 模型数据
顶点
索引
纹理
材质
Shader
        │
        ▼
   GPU Resource
 Buffer / Image
        │
        │
        ├──── VkDescriptorSet
        │
        │
        ▼
 VkGraphicsPipeline
        │
        │
        ▼
 CommandBuffer
        │
        │ vkQueueSubmit
        ▼


这个是文件流模型
从文件系统里面拿出来纹理,着色器的GLSL源码，材质，纹理等
        SSD / 硬盘
             │
             │ read()
             ↓
        CPU 系统内存 RAM
             │
             │ staging upload
             ↓
     GPU 可访问的 Device Memory
      ┌──────┼────────┐
      │      │        │
   Vertex   Texture   Index
   Buffer   Image     Buffer
      │      │        │
      └──────┼────────┘
             ↓
        Graphics Pipeline
             ↓
      Render Target Image
             ↓
       Swapchain Image
             ↓
     Presentation Engine
             ↓
          显示器

上面是原生的VK申请内存 结合shader进行了说明

也就是说我们申请内存其实很麻烦


VMA 是 Vulkan Memory Allocator，AMD/GPUOpen 提供的一个 Vulkan 内存管理库。

在vulkan整个结构里面看
你的 Renderer / Engine
        │
        ▼
 VkBuffer / VkImage
        │
        ▼
       VMA
        │
        ├── 选择 Memory Type
        ├── 分配 VkDeviceMemory
        ├── Suballocation
        ├── bindMemory
        ├── map/unmap
        ├── 内存池
        ├── Budget
        └── Defragmentation
        │
        ▼
 Vulkan Memory API
        │
        ▼
 VkDeviceMemory
        │
        ▼
 VRAM / System RAM


 1. 为什么 Vulkan 需要 VMA

如果不用 VMA，你自己创建一个 Vertex Buffer，大概得做：

vkCreateBuffer
      ↓
vkGetBufferMemoryRequirements
      ↓
找到合适的 memoryTypeIndex
      ↓
vkAllocateMemory
      ↓
vkBindBufferMemory

例如：

VkBuffer buffer;
vkCreateBuffer(device, &bufferInfo, nullptr, &buffer);

VkMemoryRequirements requirements;
vkGetBufferMemoryRequirements(
    device,
    buffer,
    &requirements
);

uint32_t memoryType =
    findMemoryType(
        requirements.memoryTypeBits,
        VK_MEMORY_PROPERTY_DEVICE_LOCAL_BIT
    );

VkMemoryAllocateInfo allocInfo{};
allocInfo.allocationSize = requirements.size;
allocInfo.memoryTypeIndex = memoryType;

VkDeviceMemory memory;
vkAllocateMemory(
    device,
    &allocInfo,
    nullptr,
    &memory
);

vkBindBufferMemory(
    device,
    buffer,
    memory,
    0
);

表面上只是几步。

真正麻烦的是：

如果我有：

10,000 个 Mesh
20,000 个 Buffer
5,000 个 Texture

怎么办？

你肯定不能简单：

一个 Buffer
=
一个 vkAllocateMemory

这样会产生严重问题。

2. 为什么不能每个 Buffer 都 vkAllocateMemory

这是理解 VMA 的关键。

GPU Driver 的：

vkAllocateMemory()

不是普通：

malloc()

它是重量级操作。

而且 Vulkan / GPU 通常对：

VkDeviceMemory Allocation 数量

有一定限制。

假设：

Mesh A
→ 1MB

Mesh B
→ 2MB

Mesh C
→ 500KB

最笨的方法：

VkDeviceMemory A = 1MB
VkDeviceMemory B = 2MB
VkDeviceMemory C = 500KB

如果对象很多：

10000 buffers
→ 10000 VkDeviceMemory

这非常糟糕。

正确思路应该是：

申请一个大的 VkDeviceMemory

例如：

256MB

然后：

VkDeviceMemory 256MB
│
├── Buffer A  0MB   ~ 1MB
│
├── Buffer B  1MB   ~ 3MB
│
├── Buffer C  3MB   ~ 3.5MB
│
├── Texture A 4MB   ~ 20MB
│
└── ...

这叫：

Suballocation

而 VMA 最核心的价值，就是帮你做这个。

3. VMA 的核心思想

你可以把 VMA 理解为：

GPU 版 malloc/free

但比普通 malloc 多很多 Vulkan 专属逻辑。

VMA：

VmaAllocator
      │
      ▼
大块 VkDeviceMemory
      │
      ├── Allocation A
      ├── Allocation B
      ├── Allocation C
      └── Allocation D

例如：

VkDeviceMemory
256 MB
────────────────────────────────────

0MB
│
├──── VertexBuffer A 4MB
│
├──── VertexBuffer B 8MB
│
├──── Texture A      32MB
│
├──── UniformBuffer  1MB
│
├──── Free           10MB
│
└──── Texture B      64MB

VMA 管理：

哪些区域用了
哪些区域空闲
哪些 allocation 属于哪个 resource
怎么对齐
怎么重用空洞
4. VMA 最核心的对象

VMA 其实结构并不复杂。

你主要会遇到 4 个对象：

VmaAllocator

VmaAllocation

VmaAllocationInfo

VmaPool
5. VmaAllocator 是什么

这是整个 VMA 的总管理器。

可以理解成：

class VmaAllocator
{
    VkPhysicalDevice physicalDevice;
    VkDevice device;
    VkInstance instance;

    MemoryTypes memoryTypes[];

    MemoryHeaps memoryHeaps[];

    Pools pools[];

    AllocationMetadata metadata;
};

当然内部实际比这个复杂。

创建：

VmaAllocator allocator;

VmaAllocatorCreateInfo info{};
info.instance = instance;
info.physicalDevice = physicalDevice;
info.device = device;

vmaCreateAllocator(
    &info,
    &allocator
);

之后你的绝大多数内存操作都走：

allocator

所以：

VkDevice
    │
    ▼
VmaAllocator
    │
    ├── Buffer allocations
    ├── Image allocations
    ├── Pools
    └── Memory blocks
6. VmaAllocation 是什么

这是特别重要的概念。

假设：

VkDeviceMemory 256MB

里面某一段：

offset = 32MB
size   = 8MB

VMA 会用：

VmaAllocation

代表这段分配。

所以：

VkBuffer

和：

VmaAllocation

不是一个东西。

一个是：

GPU Resource

另一个是：

这个 Resource 后面对应的内存分配。

例如：

VkBuffer buffer;
VmaAllocation allocation;

关系：

VkBuffer
    │
    │ bind
    ▼
VmaAllocation
    │
    ▼
某块 VkDeviceMemory
offset = xxxx
size   = xxxx
7. VMA 最爽的地方：直接创建 Buffer

不用 VMA：

vkCreateBuffer

vkGetBufferMemoryRequirements

findMemoryType

vkAllocateMemory

vkBindBufferMemory

用了 VMA：

VkBufferCreateInfo bufferInfo{};

VmaAllocationCreateInfo allocInfo{};

VkBuffer buffer;
VmaAllocation allocation;

vmaCreateBuffer(
    allocator,
    &bufferInfo,
    &allocInfo,
    &buffer,
    &allocation,
    nullptr
);

结束。

VMA 内部帮你做：

vkCreateBuffer
      ↓
vkGetBufferMemoryRequirements
      ↓
选择 memoryType
      ↓
寻找已有 Memory Block
      ↓
找到空闲区域
      ↓
Suballocate
      ↓
vkBindBufferMemory
8. Image 同样

不用 VMA：

vkCreateImage
↓
vkGetImageMemoryRequirements
↓
memoryType
↓
vkAllocateMemory
↓
vkBindImageMemory

VMA：

vmaCreateImage(
    allocator,
    &imageInfo,
    &allocInfo,
    &image,
    &allocation,
    nullptr
);

所以通常：

VkBuffer   + VmaAllocation

VkImage    + VmaAllocation

成对出现。

9. VMA 内部真正的结构

你问“结构是怎样的”，这一层最值得理解。

大体可以理解成：

                    VmaAllocator
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
      MemoryType 0   MemoryType 1   MemoryType 2
          │              │              │
          ▼              ▼              ▼
      Block List      Block List      Block List
          │
      ┌───┼───────────────┐
      ▼   ▼               ▼
    Block A             Block B
  VkDeviceMemory       VkDeviceMemory
      │                  │
      │                  │
 ┌────┼─────┐       ┌────┼─────┐
 ▼    ▼     ▼       ▼    ▼     ▼
Alloc Alloc Free    Alloc Free Alloc

最底层其实就是：

Memory Type
    ↓
Memory Block
    ↓
Suballocation
10. Memory Heap 和 Memory Type

这部分其实和你之前 Vulkan Memory 的知识直接衔接。

GPU 可能有：

Heap 0
DEVICE_LOCAL
8GB VRAM

Heap 1
HOST RAM
32GB

然后每个 Heap 可能暴露多个：

Memory Type

例如：

MemoryType 0

DEVICE_LOCAL

适合：

Vertex Buffer
Index Buffer
Texture
Render Target

例如：

MemoryType 1

HOST_VISIBLE
HOST_COHERENT

适合：

Staging Buffer
Uniform Upload
CPU → GPU

VMA 就是在帮你做：

Resource Requirements
        +
Allocation Requirements
        ↓
选择最佳 Memory Type

所以你很多时候不用自己：

findMemoryType()
11. VMAAllocationCreateInfo 是关键输入

你创建 allocation 时，会告诉 VMA：

我想怎么使用这块内存。

例如旧一些的使用方式：

allocInfo.usage = VMA_MEMORY_USAGE_AUTO;

或者：

allocInfo.flags =
    VMA_ALLOCATION_CREATE_HOST_ACCESS_SEQUENTIAL_WRITE_BIT;

例如一个 CPU 上传 Buffer：

CPU 经常写
GPU 读取

你告诉 VMA：

AUTO
+
HOST_ACCESS_SEQUENTIAL_WRITE

VMA 帮你寻找：

HOST_VISIBLE
可能 HOST_COHERENT
同时尽量适合 GPU 使用

而对于：

Vertex Buffer
Texture

通常希望：

DEVICE_LOCAL

VMA 就会优先考虑显存。

12. VMA 也帮你处理 Mapping

不用 VMA：

vkMapMemory(...)
memcpy(...)
vkFlushMappedMemoryRanges(...)
vkUnmapMemory(...)

用 VMA：

void* data;

vmaMapMemory(
    allocator,
    allocation,
    &data
);

memcpy(data, src, size);

vmaUnmapMemory(
    allocator,
    allocation
);

当然：

flush/invalidate 的语义仍然存在。

VMA 也提供对应 API。

它不是改变 Vulkan Memory Model。

只是帮你管理。

13. Persistent Mapping

实际引擎非常常见：

Uniform Upload Buffer
Staging Ring Buffer

创建之后：

一直 map

而不是每帧：

map
unmap
map
unmap

VMA 支持：

persistent mapping

例如 allocation 创建时要求：

MAPPED

然后：

allocationInfo.pMappedData

直接拿到 CPU 地址。

结构类似：

CPU Pointer
    │
    ▼
Mapped GPU Memory
    │
    ▼
VmaAllocation
14. VmaPool 是什么

默认情况下：

VmaAllocator

帮你自动管理所有内存。

但有些时候你想自己建一块专门的区域。

例如：

只放 transient buffer

或者：

只放某类 Texture

就可以：

VmaPool

例如：

VmaAllocator
│
├── Default Pool
│
├── Upload Pool
│
├── Texture Pool
│
└── Transient Pool

然后：

Upload Pool

专门用：

HOST_VISIBLE

而：

Texture Pool

专门：

DEVICE_LOCAL

可以理解成：

VmaPool = 你人为划出来的专用内存池。

15. 一个 Engine 里可能这样组织

例如：

RendererMemory
│
├── DeviceLocalAllocator
│
│   ├── Vertex Buffer
│
│   ├── Index Buffer
│
│   └── Texture
│
├── UploadPool
│   ├── Staging Buffer
│   └── Dynamic Uniform
│
└── ReadbackPool
    └── GPU → CPU Buffer

实际底下仍然：

VMA

来管理。

16. VMA 的 Suballocation 算法

例如一个 Memory Block：

256MB

[AAAA][BBBB][FREE][CCCC][FREE]

现在你要：

8MB

VMA 会寻找：

Free Region

然后考虑：

size
alignment
resource type
allocation strategy

放进去。

例如：

Before

AAAA BBBB ........ CCCC ........

After

AAAA BBBB DDDD.... CCCC ........

本质上跟：

malloc allocator

非常相似。

只是 GPU 内存需要处理：

alignment

buffer-image granularity

memoryType

memory heap

resource requirements

这些 Vulkan 特有约束。

17. Dedicated Allocation

有些很大的 Resource 不适合和别人挤一个 Block。

例如：

4K HDR Render Target

500MB

这时 VMA 可能：

Dedicated Allocation

也就是：

VkImage
   │
   ▼
VkDeviceMemory

整块内存专门属于它。

而不是：

VkDeviceMemory
├── Texture A
├── Buffer B
└── Render Target

VMA 会根据：

allocation size
driver requirements
resource hints

决定。

18. VMA 还能做 Budget

比如你 GPU：

VRAM 24GB

并不代表你应该一直分配到：

23.99GB

VMA 可以获取：

heap usage
heap budget

类似：

GPU VRAM

Budget:
20.3 GB

Currently Used:
12.7 GB

这样 Engine 可以做：

Texture Streaming
LRU eviction
Resource unloading
19. Defragmentation

假设长时间运行：

Allocate
Free
Allocate
Free

最终：

Memory

AAAA .. BBBB . CC .... DDDD ...

可能还有：

总 free = 100MB

但是：

最大连续 free = 20MB

于是：

50MB texture

放不进去。

这就是：

内存碎片。

VMA 支持 Defragmentation，概念上就是：

Before

[A][free][B][free][C]

↓

Move

[A][B][C][      free      ]

不过 GPU Resource 移动不像 CPU malloc 那么简单，因为资源本身还绑定着 allocation，所以这是比较高级的功能。

20. VMA 不负责什么

这个同样重要。

VMA 不负责：

CommandBuffer      ❌

Pipeline           ❌

Descriptor         ❌

Shader             ❌

Barrier            ❌

Semaphore          ❌

Fence              ❌

Queue              ❌

渲染流程           ❌

它只负责：

Memory Management

21. VMA 也不管理 Resource 生命周期语义

例如你：

GPU 还在使用 Vertex Buffer

CPU 就：

vmaDestroyBuffer(...)

一样是错的。

VMA 不会自动替你：

等待 Fence

你还是必须：

Fence signaled
↓
确认 GPU 不再使用
↓
vmaDestroyBuffer

所以：

GPU synchronization

还是 Vulkan 自己的规则。

22. VMA 也不替代 Barrier

例如：

CPU 写 mapped memory

↓

GPU 使用

或者：

Transfer 写 Vertex Buffer

↓

Vertex Shader / Vertex Input 读

VMA 负责：

内存从哪里分

而：

什么时候可以读

仍然是：

Vulkan synchronization

所以：

VMA
=
Memory Allocation

Barrier
=
Memory Dependency

完全不同。

23. 一个完整的 Vertex Buffer 流程

现在你可以把 VMA 插进你原有认知。

CPU
Mesh Data
    │
    ▼
VMA 创建 Staging Buffer
    │
    ▼
CPU memcpy

然后：

VMA 创建 Device Local Vertex Buffer

然后：

CommandBuffer

vkCmdCopyBuffer

然后：

Barrier

最后：

Vertex Input

所以：

                    CPU
                     │
                     ▼
               Mesh Vertex Data
                     │
                     ▼
                 VMA
                     │
             ┌───────┴────────┐
             ▼                ▼
       Staging Buffer     Vertex Buffer
       HOST_VISIBLE       DEVICE_LOCAL
             │                ▲
             │                │
             └─ Copy Command ─┘
                              │
                              ▼
                           Barrier
                              │
                              ▼
                         Vertex Input

你会发现：

VMA 只参与“Buffer 后面的 memory”。

完全不会进入：

Pipeline / Draw

那一层。

24. 用你现在的 Vulkan 七层结构来看

之前我们可以把 Vulkan 分：

1 Resource

2 Memory

3 Binding

4 Pipeline

5 Command

6 Submission

7 Synchronization

VMA 就位于：

1 Resource
     │
     ▼
2 Memory

更准确：

Resource
VkBuffer
VkImage

      ↓

VMA

      ↓

Memory
VkDeviceMemory
25. VMA 在实际引擎里的位置

你可以这样设计：

class VulkanMemoryManager
{
private:
    VmaAllocator allocator;

public:

    Buffer createBuffer(...);

    Image createImage(...);

    void destroyBuffer(...);

    void destroyImage(...);

    void* map(...);
};

例如：

struct Buffer
{
    VkBuffer buffer;

    VmaAllocation allocation;

    VkDeviceSize size;
};

Image：

struct Image
{
    VkImage image;

    VkImageView view;

    VmaAllocation allocation;
};

然后 Renderer 上层根本不关心：

VkDeviceMemory

甚至可能完全不出现：

VkDeviceMemory
26. 这是 VMA 真正改变代码结构的地方

裸 Vulkan：

Renderer

↓
VkBuffer
↓
Memory Requirements
↓
findMemoryType
↓
VkDeviceMemory
↓
Bind

VMA：

Renderer

↓
VMA

↓
Buffer + Allocation

所以很多 Vulkan Engine 里你会发现：

VkDeviceMemory

几乎很少自己直接操作。

全部交给 VMA。

27. 你可以把 VMA 类比 Java

你 Java 背景比较深，可以用这个类比。

裸 Vulkan 很像：

自己调用 OS virtual memory API

VirtualAlloc
mmap
页管理
对齐
内存池

VMA 更像：

malloc / JVM allocator

你告诉它：

我要 8MB
用途是 GPU Device Local

然后：

Allocator

决定实际：

哪块大内存
哪个 offset
哪种 memory type
28. 但有一个非常重要的区别

不要理解成：

VmaAllocation
=
VkDeviceMemory

通常不是。

更准确：

VmaAllocation
      │
      │ offset
      ▼
VkDeviceMemory Block

例如：

VkDeviceMemory 256MB
│
├── VmaAllocation A
│      offset 0MB
│      size 4MB
│
├── VmaAllocation B
│      offset 4MB
│      size 32MB
│
└── VmaAllocation C
       offset 36MB
       size 8MB

这个就是理解 VMA 最核心的一张图。

29. 所以你最终可以这样记
VkBuffer / VkImage
       │
       │ 需要 backing memory
       ▼
VMA
       │
       ├── 找 Memory Type
       │
       ├── 找 Memory Block
       │
       ├── Suballocate
       │
       ├── Alignment
       │
       ├── Mapping
       │
       └── Bind
       ▼
VkDeviceMemory
       │
       ▼
VRAM / RAM

所以：

Vulkan 告诉你“内存怎么工作的”。

而：

VMA 帮你把这一套繁琐、容易写错、需要大量工程化的内存分配逻辑封装起来。

如果你已经理解 Vulkan memory type / heap / VkDeviceMemory / host-visible / device-local，那么 VMA 本身其实不用学很久。最值得继续看的反而是：

VmaAllocator
    ↓
Memory Block
    ↓
Suballocation
    ↓
VmaAllocation

这条内部结构。理解它以后，VMA 绝大多数 API 都会很自然。



最后
再看一遍内存过程
再看 CPU 到 GPU 上传顶点的完整过程

例如静态 Vertex Buffer，常见方式：

CPU
 │
 │ memcpy
 ▼
Staging Buffer
HOST_VISIBLE memory
 │
 │ vkCmdCopyBuffer
 ▼
Device Local Vertex Buffer
 │
 │ barrier
 ▼
Vertex Input
 │
 ▼
Vertex Shader

CommandBuffer：

vkCmdCopyBuffer(
    cmd,
    stagingBuffer,
    vertexBuffer
);

VkBufferMemoryBarrier2 barrier {
   srcStage  = TRANSFER,
   srcAccess = TRANSFER_WRITE,

   dstStage  = VERTEX_INPUT,
   dstAccess = VERTEX_ATTRIBUTE_READ
};

vkCmdPipelineBarrier2(...);

含义：

Copy Buffer 写完成
        ↓
结果 available / visible
        ↓
Vertex Input 才能读取






